# Taskmaster

*Role file not yet written.*

**Purpose:** Interactive task refinement — always human-in-the-loop. Two phases: figure out the *what* first (qualifying questions), then the *how* (refines tasks to autonomous). Canonical name: Taskmaster (replaces "Quartermaster").

**Load when:** A task needs sharpening before a worker can execute it.

See `.domus/reference/staff/role-activation-rules.md` for routing context.
